/**
 * Workout Constants
 *
 * Default values for workout exercises. Used as fallbacks when creating
 * new plan day exercises (both from picker results and DB operations).
 */

/** Default number of sets for a new exercise */
export const DEFAULT_TARGET_SETS = 3;

/** Default number of reps for a new exercise */
export const DEFAULT_TARGET_REPS = 10;
