/**
 * Database & API Constants
 *
 * Shared values for pagination, debounce, and data fetching.
 *
 * USAGE:
 * import { DEFAULT_PAGE_SIZE, SEARCH_DEBOUNCE_MS } from '@/constants';
 */

/** Default number of items per page/batch for exercise queries (50) */
export const DEFAULT_PAGE_SIZE = 50;

/** Debounce delay for search input (300ms) */
export const SEARCH_DEBOUNCE_MS = 300;
