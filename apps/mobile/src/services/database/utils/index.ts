/**
 * Database Utilities - Barrel Export
 *
 * Shared utilities for database operations: auth validation and error handling.
 *
 * USAGE:
 * import { requireAuth, withDatabaseError } from '../utils';
 */

export * from './requireAuth';
export * from './withDatabaseError';
